[project]
name = "ts-daemon"
language = "typescript"

[build]
mode = "monolith"
agent = "claude_code"
assemble = "生成一个后台服务。支持命令行参数 start（启动并进入后台），stop（发送停止信号），status（查看状态）。启动后监听 stdin 命令：'submit {json}' 提交任务，'list' 查看队列，'quit' 退出。使用 Node.js 进程间通信或 pid 文件管理后台进程。"
