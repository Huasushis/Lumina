[project]
name = "ts-daemon"
language = "typescript"

[build]
mode = "monolith"
agent = "claude_code"
timeout = 900
assemble = "后台任务队列服务。支持：start（后台启动，监听 localhost:9123），stop（发 POST /__shutdown），status（查看状态），connect（交互客户端，发命令到服务器）。submit {json} 提交任务，list 查看队列，remove {id} 删除任务。"
