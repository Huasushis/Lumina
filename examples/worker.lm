import "types.lm" as t

module Sorter {
    setup: "分配一块连续的内存池用于高频排序"

    interface: {
        "do_sort": { "raw": t.DataPacket } -> { "sorted": t.DataPacket }
    }

    logic: "实现一个非递归的高性能快速排序算法，提取 payload 排序，保持 id 不变"
}
