[SYSTEM]
You are assembling a Lumina Actor Model system into a runnable application.
All individual modules have been implemented. Your job: create the glue code.

[MODULES IMPLEMENTED]
{% for name, info in modules.items() %}
Module: {{ name }}
  Files:
{% for f in info.files %}
    - {{ f.path }}
{% endfor %}
{% endfor %}

[SYSTEM TOPOLOGY]
Dependency order (leaf modules first):
{% for name in dependency_order %}
  {{ loop.index }}. {{ name }}
{% endfor %}

{% if actor_graph %}
Actor wiring:
{% for mod, children in actor_graph.items() %}
  {{ mod }}:
{% for child in children %}
    -> {{ child.actor_name }} (type: {{ child.module_type }})
{% endfor %}
{% endfor %}
{% endif %}

{% if assemble_hint %}
[CUSTOM ASSEMBLY REQUIREMENT]
{{ assemble_hint }}
{% else %}
[DEFAULT REQUIREMENT]
Create a runnable entry point that:
1. Imports all modules
2. Creates a message router
3. Registers all actors
4. Calls run() on each
5. Provides an interactive REPL that accepts "Actor.method {json}" commands
6. On "quit", cleanly shuts down

Also create the runtime base class (LuminaActor) if modules import from it.
Use idiomatic conventions for the target language. Do NOT use any framework — keep it minimal.
{% endif %}

[OUTPUT FORMAT]
Respond ONLY with a JSON object containing ALL files needed:
{
  "files": [
    {"path": "relative/path", "content": "file content"},
    ...
  ]
}

Include: runtime base class, message router, main entry point, package config if needed.
Write complete, working code. No placeholders.
